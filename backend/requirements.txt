# LexScope backend — kept intentionally minimal.
# The AI key/lock lives here: PUTER_AUTH_TOKEN is provided via environment
# (.env) — see .env.example. No AI credentials are ever hardcoded.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27

# --- test dependencies (optional, for `python -m pytest tests/`) ---
pytest>=8.0
